SELECT imiepracownik, nazwiskopracownik, nazwadzial, wiek FROM pracownik INNER JOIN dzial ON pracownik.dzial = dzial.iddzial 
WHERE wiek IN (SELECT MAX(wiek) FROM pracownik INNER JOIN dzial ON pracownik.dzial = dzial.iddzial GROUP BY dzial)